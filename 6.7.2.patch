diff --git a/.github/workflows/close-preview.yml b/.github/workflows/close-preview.yml
index 31ff6456..1ccaf53d 100644
--- a/.github/workflows/close-preview.yml
+++ b/.github/workflows/close-preview.yml
@@ -1,4 +1,5 @@
 name: Close preview environment
+
 on:
   workflow_dispatch:
     inputs:
@@ -7,15 +8,33 @@ on:
         type: string
         description: PR number
   pull_request_target:
-    types: [closed]
+    types:
+      - closed
+
+permissions:
+  contents: read
+  id-token: write
 
 jobs:
   close:
     runs-on: ubuntu-latest
     if: github.repository == 'microsoft/TypeScript-Website'
     steps:
-      - uses: Azure/static-web-apps-deploy@v1
+      - uses: azure/login@v2
+        with:
+          client-id: ${{ secrets.AZURE_CLIENT_ID }}
+          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
+          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
+      - name: Remove preview from Azure Storage
+        uses: azure/CLI@v2
+        env:
+          AZURE_STORAGE_ACCOUNT: ${{ vars.AZURE_STORAGE_ACCOUNT }}
+          PR_NUMBER: ${{ github.event.inputs.pr || github.event.pull_request.number }}
         with:
-          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PREVIEW }}
-          action: close
-          app_location: /dev/null
+          inlineScript: |
+            set -euo pipefail
+            az storage blob delete-batch \
+              --account-name "$AZURE_STORAGE_ACCOUNT" \
+              --source '$web' \
+              --pattern "pr-$PR_NUMBER/*" \
+              --auth-mode login || true
diff --git a/.github/workflows/deploy-preview.yml b/.github/workflows/deploy-preview.yml
index f6ec32dd..bd97e20d 100644
--- a/.github/workflows/deploy-preview.yml
+++ b/.github/workflows/deploy-preview.yml
@@ -1,189 +1,377 @@
 name: Deploy preview environment
+
 on:
-  workflow_run:
-    workflows: [CI]
-    types: [completed]
-  workflow_dispatch:
-    inputs:
-      pr:
-        required: true
-        type: string
-        description: PR number
-  pull_request_target:
-    types: [labeled]
+  pull_request:
+    branches:
+      - v2
+    types:
+      - opened
+      - synchronize
+      - reopened
+      - ready_for_review
 
 permissions:
   contents: read
   pull-requests: write
-  actions: read
+  id-token: write
+
+concurrency:
+  group: pr-preview-${{ github.event.pull_request.number }}
+  cancel-in-progress: true
 
 jobs:
-  deploy:
+  detect-affected:
     runs-on: ubuntu-latest
-    if: github.repository == 'microsoft/TypeScript-Website'
+    outputs:
+      affected_packages: ${{ steps.detect.outputs.affected_packages }}
+      lint_packages: ${{ steps.detect.outputs.lint_packages }}
+      test_packages: ${{ steps.detect.outputs.test_packages }}
+      typecheck_targets: ${{ steps.detect.outputs.typecheck_targets }}
+      should_build_site: ${{ steps.detect.outputs.should_build_site }}
     steps:
-      - name: Get PR/workflow run info
-        id: get-info
-        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
+      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
         with:
-          script: |
-            console.dir(context, { depth: null });
-
-            let pr;
-            let workflowRun;
-            let isLabel = false;
-            switch (context.eventName) {
-              case "workflow_dispatch":
-                console.log("Workflow dispatch event");
-                pr = (await github.rest.pulls.get({
-                  owner: context.repo.owner,
-                  repo: context.repo.repo,
-                  pull_number: +context.payload.inputs.pr,
-                })).data;
-                break;
-              case "pull_request_target":
-                console.log("Pull request target event");
-                pr = context.payload.pull_request;
-                break;
-              case "workflow_run":
-                console.log("Workflow run event");
-                workflowRun = context.payload.workflow_run;
-                pr = workflowRun.pull_requests[0];
-                if (pr) {
-                  console.log("PR found in workflow run");
-                  // Reload the PR to get the labels.
-                  pr = (await github.rest.pulls.get({
-                    owner: context.repo.owner,
-                    repo: context.repo.repo,
-                    pull_number: pr.number,
-                  })).data;
-                } else {
-                  const q = `is:pr is:open repo:${context.repo.owner}/${context.repo.repo} sha:${workflowRun.head_sha}`;
-                  console.log(`PR not found in workflow run, searching for it with ${q}`);
-                  // PRs sent from forks do not get the pull_requests field set.
-                  // https://github.com/orgs/community/discussions/25220
-                  const response = await github.rest.search.issuesAndPullRequests({ q });
-                  if (response.data.items.length > 0) {
-                    pr = response.data.items[0];
-                  }
-                }
-            }
+          fetch-depth: 0
+      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
+        with:
+          node-version: "20.x"
+          cache: pnpm
+      - uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e
+      - name: Install dependencies
+        run: pnpm install --frozen-lockfile
+      - name: Collect affected packages
+        id: detect
+        env:
+          BASE_REF: ${{ github.event.pull_request.base.ref }}
+        run: |
+          git fetch --no-tags origin "$BASE_REF" --depth=1
+          git diff --name-only "origin/$BASE_REF...HEAD" > changed-files.txt
+          pnpm changeset status --since="origin/$BASE_REF" --output=changeset-status.json || true
+          pnpm --filter "...[origin/$BASE_REF]" list --depth -1 --json > pnpm-affected.json || echo "[]" > pnpm-affected.json
+          node - <<'NODE'
+          const fs = require("fs")
+          const path = require("path")
 
-            if (!pr) {
-              console.log("Could not find PR");
-              return null;
-            }
+          const packagesRoot = path.join(process.cwd(), "packages")
+          const changedFiles = fs.existsSync("changed-files.txt")
+            ? fs.readFileSync("changed-files.txt", "utf8").split(/\r?\n/).filter(Boolean)
+            : []
 
-            console.log(`Found PR ${pr.html_url}`);
-            console.dir(pr, { depth: null });
+          const workspacePackages = new Map()
+          for (const entry of fs.readdirSync(packagesRoot, { withFileTypes: true })) {
+            if (!entry.isDirectory()) continue
+            const packageJsonPath = path.join(packagesRoot, entry.name, "package.json")
+            if (!fs.existsSync(packageJsonPath)) continue
+            const manifest = JSON.parse(fs.readFileSync(packageJsonPath, "utf8"))
+            workspacePackages.set(manifest.name, {
+              name: manifest.name,
+              dir: path.join("packages", entry.name),
+              scripts: manifest.scripts || {},
+              hasTsconfig: fs.existsSync(path.join(packagesRoot, entry.name, "tsconfig.json")),
+              hasTsconfigLib: fs.existsSync(path.join(packagesRoot, entry.name, "tsconfig.lib.json")),
+            })
+          }
 
-            if (pr.state !== "open") {
-              console.log(`PR ${pr.number} is not open`);
-              return null;
-            }
+          const changesetStatus = fs.existsSync("changeset-status.json")
+            ? JSON.parse(fs.readFileSync("changeset-status.json", "utf8"))
+            : {}
+          const pnpmAffected = fs.existsSync("pnpm-affected.json")
+            ? JSON.parse(fs.readFileSync("pnpm-affected.json", "utf8"))
+            : []
 
-            if (!pr.labels.some((label) => label.name === "deploy-preview")) {
-              console.log(`PR ${pr.number} does not have the deploy-preview label`);
-              return null;
-            }
+          const affectedNames = new Set([
+            ...(changesetStatus.releases || []).map(release => release.name),
+            ...pnpmAffected.map(entry => entry.name),
+          ].filter(Boolean))
+
+          const affectedPackages = [...affectedNames]
+            .map(name => workspacePackages.get(name))
+            .filter(Boolean)
+            .sort((a, b) => a.name.localeCompare(b.name))
+
+          const lintPackages = affectedPackages
+            .filter(pkg => Boolean(pkg.scripts.lint))
+            .map(pkg => pkg.name)
 
-            if (!workflowRun) {
-              console.log(`No workflow run found in event, searching for it with ${pr.head.sha}`);
-              try {
-                workflowRun = (await github.rest.actions.listWorkflowRuns({
-                  owner: context.repo.owner,
-                  repo: context.repo.repo,
-                  workflow_id: "CI.yml",
-                  head_sha: pr.head.sha,
-                  per_page: 1,
-                })).data.workflow_runs[0];
-              } catch (e) {
-                console.log(e);
+          const testPackages = affectedPackages
+            .filter(pkg => Boolean(pkg.scripts.test))
+            .map(pkg => pkg.name)
+
+          const typecheckTargets = affectedPackages
+            .map(pkg => {
+              const script = ["type-check", "typecheck", "tsc", "compile"].find(name => Boolean(pkg.scripts[name]))
+              if (script) {
+                return { name: pkg.name, mode: "script", script }
+              }
+              if (pkg.hasTsconfig) {
+                return { name: pkg.name, mode: "exec", project: "tsconfig.json" }
+              }
+              if (pkg.hasTsconfigLib) {
+                return { name: pkg.name, mode: "exec", project: "tsconfig.lib.json" }
               }
+              return null
+            })
+            .filter(Boolean)
+
+          const websitePackage = workspacePackages.get("typescriptlang-org")
+          const websiteWorkspaceDeps = websitePackage
+            ? Object.keys(JSON.parse(fs.readFileSync(path.join(websitePackage.dir, "package.json"), "utf8")).dependencies || {})
+                .filter(name => workspacePackages.has(name))
+            : []
+          const siteContentPackages = ["documentation", "glossary", "tsconfig-reference", "community-meta"]
+          const siteTriggerFiles = new Set([
+            "package.json",
+            "pnpm-workspace.yaml",
+            "pnpm-lock.yaml",
+            ".changeset/config.json",
+            ".github/workflows/deploy-preview.yml",
+          ])
+          const siteTriggerDirs = [
+            "packages/typescriptlang-org/",
+            "packages/documentation/",
+            "packages/glossary/",
+            "packages/tsconfig-reference/",
+            "packages/community-meta/",
+          ]
+
+          const shouldBuildSite =
+            affectedPackages.some(pkg => ["typescriptlang-org", ...websiteWorkspaceDeps, ...siteContentPackages].includes(pkg.name)) ||
+            changedFiles.some(file => siteTriggerFiles.has(file) || siteTriggerDirs.some(dir => file.startsWith(dir)))
+
+          const appendOutput = (key, value) => {
+            fs.appendFileSync(process.env.GITHUB_OUTPUT, `${key}=${JSON.stringify(value)}\n`)
+          }
+
+          appendOutput("affected_packages", affectedPackages.map(pkg => pkg.name))
+          appendOutput("lint_packages", lintPackages)
+          appendOutput("test_packages", testPackages)
+          appendOutput("typecheck_targets", typecheckTargets)
+          appendOutput("should_build_site", shouldBuildSite)
+          NODE
+
+  lint:
+    runs-on: ubuntu-latest
+    needs: detect-affected
+    steps:
+      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
+      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
+        with:
+          node-version: "20.x"
+          cache: pnpm
+      - uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e
+      - name: Install dependencies
+        run: pnpm install --frozen-lockfile
+      - name: Bootstrap workspace
+        run: |
+          pnpm docs-sync pull microsoft/TypeScript-Website-localizations#main 1
+          pnpm bootstrap
+      - name: Run lint
+        env:
+          LINT_PACKAGES: ${{ needs.detect-affected.outputs.lint_packages }}
+        run: |
+          node - <<'NODE'
+          const { spawnSync } = require("child_process")
+
+          const packages = JSON.parse(process.env.LINT_PACKAGES)
+          if (!packages.length) {
+            console.log("No affected packages expose a lint script")
+            process.exit(0)
+          }
+
+          for (const pkg of packages) {
+            console.log(`\n> pnpm --filter ${pkg} run lint`)
+            const result = spawnSync("pnpm", ["--filter", pkg, "run", "lint"], { stdio: "inherit" })
+            if (result.status !== 0) {
+              process.exit(result.status || 1)
             }
+          }
+          NODE
 
-            if (!workflowRun) {
-              console.log(`Could not find workflow run for PR ${pr.number}`);
-              return null;
+  type-check:
+    runs-on: ubuntu-latest
+    needs: detect-affected
+    steps:
+      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
+      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
+        with:
+          node-version: "20.x"
+          cache: pnpm
+      - uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e
+      - name: Install dependencies
+        run: pnpm install --frozen-lockfile
+      - name: Bootstrap workspace
+        run: |
+          pnpm docs-sync pull microsoft/TypeScript-Website-localizations#main 1
+          pnpm bootstrap
+      - name: Run type-check
+        env:
+          TYPECHECK_TARGETS: ${{ needs.detect-affected.outputs.typecheck_targets }}
+        run: |
+          node - <<'NODE'
+          const { spawnSync } = require("child_process")
+
+          const targets = JSON.parse(process.env.TYPECHECK_TARGETS)
+          if (!targets.length) {
+            console.log("No affected packages expose a type-check entrypoint")
+            process.exit(0)
+          }
+
+          for (const target of targets) {
+            const args = target.mode === "script"
+              ? ["--filter", target.name, "run", target.script]
+              : ["--filter", target.name, "exec", "tsc", "--noEmit", "-p", target.project]
+
+            console.log(`\n> pnpm ${args.join(" ")}`)
+            const result = spawnSync("pnpm", args, { stdio: "inherit" })
+            if (result.status !== 0) {
+              process.exit(result.status || 1)
             }
+          }
+          NODE
 
-            console.log(`Found workflow run ${workflowRun.html_url}`);
-            console.dir(workflowRun, { depth: null });
-
-            try {
-              const artifacts = await github.rest.actions.listWorkflowRunArtifacts({
-                owner: "microsoft",
-                repo: "TypeScript-Website",
-                run_id: workflowRun.id,
-              });
-              console.dir(artifacts, { depth: null });
-              if (!artifacts.data.artifacts.some(x => x.name === "site")) {
-                console.log("No artifact found in workflow run");
-                return null;
-              }
-            } catch (e) {
-              console.log(e);
-              return null;
+  test:
+    runs-on: ubuntu-latest
+    needs: detect-affected
+    steps:
+      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
+      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
+        with:
+          node-version: "20.x"
+          cache: pnpm
+      - uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e
+      - name: Install dependencies
+        run: pnpm install --frozen-lockfile
+      - name: Bootstrap workspace
+        run: |
+          pnpm docs-sync pull microsoft/TypeScript-Website-localizations#main 1
+          pnpm bootstrap
+      - name: Run test
+        env:
+          TEST_PACKAGES: ${{ needs.detect-affected.outputs.test_packages }}
+        run: |
+          node - <<'NODE'
+          const { spawnSync } = require("child_process")
+
+          const packages = JSON.parse(process.env.TEST_PACKAGES)
+          if (!packages.length) {
+            console.log("No affected packages expose a test script")
+            process.exit(0)
+          }
+
+          for (const pkg of packages) {
+            console.log(`\n> pnpm --filter ${pkg} run test`)
+            const result = spawnSync("pnpm", ["--filter", pkg, "run", "test"], {
+              stdio: "inherit",
+              env: { ...process.env, CI: "true" },
+            })
+            if (result.status !== 0) {
+              process.exit(result.status || 1)
             }
+          }
+          NODE
 
-            const result = { pr: pr.number, runId: workflowRun.id };
-            console.log(result);
-            return result;
+  build-site:
+    runs-on: ubuntu-latest
+    needs:
+      - detect-affected
+      - lint
+      - type-check
+      - test
+    if: ${{ fromJson(needs.detect-affected.outputs.should_build_site) }}
+    steps:
+      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
+        with:
+          fetch-depth: 0
+      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
+        with:
+          node-version: "20.x"
+          cache: pnpm
+      - uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e
+      - name: Install dependencies
+        run: pnpm install --frozen-lockfile
+      - name: Build website
+        run: |
+          pnpm docs-sync pull microsoft/TypeScript-Website-localizations#main 1
+          pnpm bootstrap
+          pnpm run --filter=typescriptlang-org setup-playground-cache-bust
+          pnpm run --filter=typescriptlang-org build
+      - name: Upload site artifact
+        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
+        with:
+          name: site
+          path: packages/typescriptlang-org/public
 
-      - name: Download site build from PR
-        if: ${{ steps.get-info.outputs.result != 'null' }}
-        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
+  deploy:
+    runs-on: ubuntu-latest
+    needs:
+      - detect-affected
+      - build-site
+    if: ${{ fromJson(needs.detect-affected.outputs.should_build_site) && github.event.pull_request.head.repo.full_name == github.repository }}
+    steps:
+      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
         with:
           name: site
           path: site
-          github-token: ${{ secrets.GITHUB_TOKEN }}
-          run-id: ${{ fromJson(steps.get-info.outputs.result).runId }}
-
-      - name: Deploy
-        id: deploy
-        if: ${{ steps.get-info.outputs.result != 'null' }}
-        uses: Azure/static-web-apps-deploy@v1
+      - uses: azure/login@v2
+        with:
+          client-id: ${{ secrets.AZURE_CLIENT_ID }}
+          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
+          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
+      - name: Upload preview to Azure Storage
+        uses: azure/CLI@v2
+        env:
+          AZURE_STORAGE_ACCOUNT: ${{ vars.AZURE_STORAGE_ACCOUNT }}
+          PREVIEW_PREFIX: pr-${{ github.event.pull_request.number }}
         with:
-          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PREVIEW }}
-          # Unsetting the repo token so we can control commenting ourselves.
-          # repo_token: ${{ secrets.GITHUB_TOKEN }}
-          action: "upload"
-          app_location: "site"
-          skip_app_build: true
-          production_branch: v2
-          deployment_environment: ${{ fromJson(steps.get-info.outputs.result).pr }}
-
-      - name: Comment on PR
-        if: ${{ steps.get-info.outputs.result != 'null' }}
-        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
+          inlineScript: |
+            set -euo pipefail
+            az storage blob delete-batch \
+              --account-name "$AZURE_STORAGE_ACCOUNT" \
+              --source '$web' \
+              --pattern "$PREVIEW_PREFIX/*" \
+              --auth-mode login || true
+            az storage blob upload-batch \
+              --account-name "$AZURE_STORAGE_ACCOUNT" \
+              --destination '$web/'"$PREVIEW_PREFIX" \
+              --source site \
+              --overwrite \
+              --auth-mode login
+      - name: Set preview URL
+        id: preview-url
+        run: echo "preview_url=https://pr-${{ github.event.pull_request.number }}.typescriptlang.org/" >> "$GITHUB_OUTPUT"
+      - name: Comment preview URL on PR
+        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
         env:
-          PR_NUMBER: ${{ fromJson(steps.get-info.outputs.result).pr }}
-          SITE_URL: ${{ steps.deploy.outputs.static_web_app_url }}
+          PREVIEW_URL: ${{ steps.preview-url.outputs.preview_url }}
         with:
           github-token: ${{ secrets.GITHUB_TOKEN }}
           script: |
-            const prefix = "Azure Static Web Apps: Your stage site is ready!";
+            const marker = "<!-- pr-preview-link -->"
+            const body = `${marker}\nPreview environment is ready: ${process.env.PREVIEW_URL}`
+            const issue_number = context.payload.pull_request.number
             const comments = await github.paginate(github.rest.issues.listComments, {
               owner: context.repo.owner,
               repo: context.repo.repo,
-              issue_number: +process.env.PR_NUMBER,
+              issue_number,
               per_page: 100,
-            });
-
-            for (const comment of comments) {
-              if (comment.user?.login === "github-actions[bot]" && comment.body?.startsWith(prefix)) {
-                console.log(`Deleting comment ${comment.id}`);
-                await github.rest.issues.deleteComment({
-                  owner: context.repo.owner,
-                  repo: context.repo.repo,
-                  comment_id: comment.id,
-                });
-              }
-            }
+            })
 
-            await github.rest.issues.createComment({
-              owner: context.repo.owner,
-              repo: context.repo.repo,
-              issue_number: +process.env.PR_NUMBER,
-              body: `${prefix} Visit it here: ${process.env.SITE_URL}`
-            });
+            const existing = comments.find(comment =>
+              comment.user?.login === "github-actions[bot]" && comment.body?.includes(marker)
+            )
+
+            if (existing) {
+              await github.rest.issues.updateComment({
+                owner: context.repo.owner,
+                repo: context.repo.repo,
+                comment_id: existing.id,
+                body,
+              })
+            } else {
+              await github.rest.issues.createComment({
+                owner: context.repo.owner,
+                repo: context.repo.repo,
+                issue_number,
+                body,
+              })
+            }
